// Root app shell. Mounts the router. Bottom navigation (Home/Journey/Settings)
// lives in src/components/layout and wraps the section routes.
// TODO: build out the router in src/routes.tsx and the layout shell.
export default function App() {
  return <div className="font-ar" dir="rtl">{/* TODO: <AppRouter /> */}</div>;
}
